# Vercel / npm configuration
# Skip funding messages and audit during install for faster builds
fund=false
audit=false
# Use legacy peer deps to avoid false peer conflict errors
legacy-peer-deps=true
